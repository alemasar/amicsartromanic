/*const WebComponentDictionary = require("./WebComponentDictionary");
const loader_utils = require("loader-utils");
const Parser = require("../parser/Parser");
const fs = require('fs')*/

class Dictionary {
  constructor(methods) {
    this.methods = methods;
  }
}

module.exports = Dictionary;
