/* eslint-disable */
const loader_utils = require("loader-utils");
const fs = require("fs");
const path = require("path");

class HTMLTemplateMethods {
    writeTemplate(inputs, output) {
      //console.log("PASO PER WRITE TEMPLATE: ", inputs)
      return ""
    }
}

module.exports = HTMLTemplateMethods;